name: Imai test

on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
      - name: checkout
        uses: actions/checkout@master
        run: pwd & ls

  test:
    needs: build
    runs-on: ubuntu-latest
    steps: 
     - run: pwd & ls
